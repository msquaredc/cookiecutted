language: elm

sudo: required

install:
  - npm install elm-review --loglevel verbose
  - npm i create-elm-app -g
  - npm i elm-coverage -g
  #- npm i coveralls -g
script: 
  - elm-format --verify
  - elm-review
  - elm-app test
  - elm-coverage
  #- cat ./.coverage/lcov.info | coveralls
